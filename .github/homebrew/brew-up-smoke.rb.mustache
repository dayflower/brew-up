cask "brew-up-smoke" do
  version "{{version}}"

  url "{{artifacts.default.url}}"
  sha256 "{{artifacts.default.sha256}}"

  name "brew-up smoke fixture"
  desc "Fixture template for manual e2e-smoke workflow runs"
  homepage "https://example.com"
end
